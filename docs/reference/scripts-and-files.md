# Scripts and Files Reference

Quick reference for key files in this repo:

- `install.sh`: bootstrap installer.
- `bashrc_basic`: base shell configuration.
- `tmux.conf` and `tmux_dir/`: tmux setup.
- `vimrc`: vim configuration.
